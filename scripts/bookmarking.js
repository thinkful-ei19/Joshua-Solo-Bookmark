'use strict';
/*global store, api*/
//eslint-disable-next-line no-unused-vars//
const bookmarkList = (function (){

  function generateBookmarkElement (bookmark){
    let bookmarkTitle = `<span class="bookmark-item bookmark-item__checked">${bookmark.title} 
  }
});